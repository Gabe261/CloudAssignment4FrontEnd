<!DOCTYPE html>
<html>

<head>
    <title>Products</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

    <nav class="navbar navbar-dark bg-dark px-3">
        <h2 class="text-white">Shoe Store</h2>
        <a href="/cart" class="btn btn-outline-light">🛒 Cart</a>
    </nav>

    <div class="container py-4">
        <div class="row">

            <!-- Load all the products available to be added to cards -->
            <% productsWithInventory.forEach(p=> { %>
                <div class="col-md-4 mb-4">
                    <div class="card shadow-sm">

                        <img src="<%= p.image %>" class="card-img-top mt-2" style="height: 200px; object-fit: contain;">

                        <div class="card-body">
                            <h5 class="card-title">
                                <%= p.name %>
                            </h5>
                            <p class="card-text">
                                <%= p.description %>
                            </p>
                            <p class="card-text fw-bold m-0">$<%= p.price %>
                            </p>
                            <p class="card-text m-0">Quantity: <%= p.quantity %>
                            </p>
                            <p class="text-muted">SKU: <%= p.sku %>
                            </p>

                            <form method="POST" action="/addtocart">
                                <input type="hidden" name="id" value="<%= p.id %>">
                                <button class="btn btn-primary w-100" <%= p.quantity === 0 ? "disabled" : "" %>>
                                    <%= p.quantity === 0 ? "Out of Stock" : "Add to Cart" %>
                                </button>
                            </form>
                        </div>

                    </div>
                </div>
            <% }) %>
        </div>
    </div>

</body>
<footer class="container">
    <p>Created By Dany Ko (8818957), Gabe Siewert (8832384), Josh Lanesmith (8885373), Nakul Upasani (8827362), Stefan Kobetich (8832196)</p>
</footer>
</html>